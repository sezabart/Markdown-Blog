# Copy this file to .env and update values before running docker-compose
# Do NOT commit secrets to version control. This file is an example only.

# Host path that will be mounted into the container as /app/content
# Example: /mnt/hdd/documents/<user>/blog-content
BLOG_CONTENT=/mnt/hdd/documents/your-user/blog-content

# SMTP settings: SMTP_PASSWORD is read inside the container at runtime
# (smtp server/port/user are configured in config.yaml)
SMTP_PASSWORD=changeme

# Admin password used to protect send endpoint
ADMIN_PASSWORD=changeme

# Optional runtime settings
# DOMAIN (used when building absolute links in emails)
DOMAIN=seza.si
IMAGE_WIDTH=500

# Note: you can export these in your shell or use this file with docker compose --env-file .env
# Example:
#   cp .env.example .env
#   edit .env
#   docker compose up --build -d
